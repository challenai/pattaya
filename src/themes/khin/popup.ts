import base from "./base";

export const background = base.background;

export const muted = {
  border: base.muted.border,
  background: base.muted.background,
  shadow: base.muted.shadow,
  shadowBlur: base.muted.shadowBlur,
};

export const normal = {
  border: base.normal.border,
  background: base.normal.background,
  shadow: base.normal.shadow,
  shadowBlur: base.normal.shadowBlur,
};

export const active = {
  border: base.active.border,
  background: base.active.background,
  shadow: base.active.shadow,
  shadowBlur: base.active.shadowBlur,
};

export const focus = {
  border: base.focus.border,
  background: base.focus.background,
  shadow: base.focus.shadow,
  shadowBlur: base.focus.shadowBlur,
};