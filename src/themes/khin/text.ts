import base from "./base";

export const background = base.background;

export const muted = {
  color: base.muted.fontColor,
};

export const normal = {
  color: base.normal.fontColor,
};

export const active = {
  color: base.active.fontColor,
};

export const focus = {
  color: base.focus.fontColor,
};