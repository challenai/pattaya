import base from "./base";
import * as lineStyles from "./line";
import * as arrowStyles from "./arrow";

export const background = base.background;

export const normal = {
  line: lineStyles.normal,
  arrow: arrowStyles.normal,
};

export const active = {
  line: lineStyles.active,
  arrow: arrowStyles.active,
};