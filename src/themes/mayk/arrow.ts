export { default as background } from "./background";
import palette from "./palette";

export const styles = {
  normal: {
    border: palette.baby,
    background: palette.white,
  },
  active: {
    border: palette.navy,
    background: palette.white,
  },
};